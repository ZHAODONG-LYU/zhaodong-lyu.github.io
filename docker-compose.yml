services:
  personal-page:
    image: nginx:1.27-alpine
    container_name: zhaodong-personal-page
    ports:
      - "8088:80"
    volumes:
      - .:/usr/share/nginx/html:ro
      - ./docker/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
